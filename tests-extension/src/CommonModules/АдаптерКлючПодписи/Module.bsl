&Вместо("КлючПодписиJWT")
Функция Мок_КлючПодписиJWT() Экспорт
	
	ПараметрыМетода = Новый Массив; // 0 параметров
    ПрерватьВыполнение = Ложь;

    Результат = МокитоПерехват.АнализВызова(
        АдаптерКлючПодписи,
        "КлючПодписиJWT",
        ПараметрыМетода,
        ПрерватьВыполнение
    );

    Если Не ПрерватьВыполнение Тогда
        Возврат ПродолжитьВызов();   // без параметров
    Иначе
        Возврат Результат;
    КонецЕсли;
КонецФункции