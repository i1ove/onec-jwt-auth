
Процедура ВыполнитьСценарий(АдресВХ, Порты) Экспорт
	
	Токен = Порты.ЧтениеТокена.ПрочитатьТокенИзВХ(АдресВХ);
	
	Токен = СтрЗаменить(Токен, Символы.ВК, "");
	
	Если Токен = "" Тогда
		ОшибкиПриложения.ОшибкаДанных("Файл прочитан, но токен пустой.", "JWTImport.BAD_INPUT.TOKEN_EMPTY");
	КонецЕсли;
	
	Порты.ХранениеТокена.Сохранить(Токен);
	
КонецПроцедуры
