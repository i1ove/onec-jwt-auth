Функция Области(Знач Операция) Экспорт

	Если СтрДлина(СокрЛП(Строка(Операция))) = 0 Тогда
		ОшибкиПриложения.ОшибкаКонфигурации(
			"Не задана операция для политики доступа.",
			"AUTHZ.OPERATION_EMPTY"
		);
	КонецЕсли;
	
	Возврат РегистрыСведений.ПолитикиДоступаОпераций.ОбластиПоОперации(Операция);
КонецФункции