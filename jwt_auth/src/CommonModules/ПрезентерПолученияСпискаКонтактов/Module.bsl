Функция ПредставитьУспех(РезультатБизнеса, КонтекстДоступа, Корреляция, КодУспехаHTTP) Экспорт
	
	Результат = Новый Массив;
	Для Каждого Контакт Из РезультатБизнеса Цикл
		Результат.Добавить(Новый Структура("name, uuid", Контакт.Наименование, Строка(Контакт.Идентификатор)));
	КонецЦикла;
	
	Тело = Новый Структура;
	Тело.Вставить("contacts", Результат);
	
	Заголовки = Новый Соответствие;
	
	Возврат Новый Структура("КодHTTP,Тело,Заголовки", КодУспехаHTTP, Тело, Заголовки);
КонецФункции