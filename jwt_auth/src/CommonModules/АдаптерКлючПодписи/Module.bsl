Функция КлючПодписиJWT() Экспорт
	Ключ = Константы.СекретныйКлючПодписиJWT.Получить();
	
	Если ТипЗнч(Ключ) = Тип("ХранилищеЗначения") Тогда
		Ключ = Ключ.Получить();
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Ключ) Тогда
		ВызватьИсключение "Не задан закрытый ключ для подписи JWT (RS256).";
	КонецЕсли;
	
	Возврат Строка(Ключ);
КонецФункции