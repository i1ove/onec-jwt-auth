Функция СформироватьВход(Знач Запрос) Экспорт
	Параметры = Запрос.ПараметрыЗапроса;
	ВходСыр = HTTPПротоколAPI.СвязатьИПроверить(ПустаяСхема(), Параметры);
	
	Возврат Новый Структура;
КонецФункции

Функция ПустаяСхема(Знач СтрогийРежим = Истина)
	Возврат Новый Структура(
		"СтрогийРежим,Поля",
		СтрогийРежим,
		Новый Соответствие
	);
КонецФункции