
Функция ВыдатьТокенPOST(Запрос)
	ПортыБизнеса = КомпозицияАвторизацияAPI.СобратьПорты();
	ПортыДоступа = КомпозицияДоступа.СобратьПорты();
	
	Исполнитель = КонтроллерВыдачиТокенаAPI;
	Презентер = ПрезентерВыдачиТокенаAPI;
	МапперВхода = Контракт_POST_Аутентификация;
	
	Операция = ОперацииAPI.Авторизация_ВыдатьJWT();
	
	Возврат ПайплайнAPI.ВыполнитьПайплайн(Запрос, ПортыДоступа, ПортыБизнеса, Операция, Исполнитель, Презентер, МапперВхода);
КонецФункции